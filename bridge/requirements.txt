# SIRO Bridge - Python 依賴
#
# 建議用 uv 管理：
#   uv venv && source .venv/bin/activate && uv pip install -r requirements.txt
# 或傳統方式：
#   python -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt

# Web 框架
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
websockets>=12.0

# 設定管理
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0
pyyaml>=6.0          # Persona YAML 解析 (Phase 1.5+)

# 中文 NLP — emotion_parser 斷詞用，避免「天氣」誤觸「氣」(angry)
# 沒裝也能跑，會 graceful 退回 char-level substring matching
jieba>=0.42.0

# gRPC — Phase 3 bridge ↔ siro-runtime 介面（runtime_client.py）
# 沒裝也不會炸：client 是 opt-in（SIRO_RUNTIME_ENABLED=true 才會實際 import）
grpcio>=1.60.0
grpcio-tools>=1.60.0   # codegen for proto → Python stubs

# 測試
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
httpx>=0.27.0

# v0.5 抽象層：跨平台 user data / config / log path 解析
# Windows: %LOCALAPPDATA%\SIRO\
# Linux: $XDG_DATA_HOME/siro/ (default ~/.local/share/siro/)
# macOS: ~/Library/Application Support/SIRO/
platformdirs>=4.2,<5

# v0.5 Windows Service 化：pywin32 (Windows only) + psutil (cross-platform)
pywin32>=306; sys_platform == "win32"
psutil>=5.9.0

# Phase 1.5: F5-TTS 本地高品質 TTS (零樣本聲線 clone)
# 沒裝也能跑,會 fallback edge-tts → Piper。詳細見 docs/TTS_F5_INTEGRATION.md
# 安裝 PyTorch(必要、CPU or GPU 擇一):
#   pip install torch torchaudio
#   # 或 GPU: pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121
f5-tts>=2024.10.0
soundfile>=0.12.1
vocos>=0.1.0
jieba>=0.42.1
# pyarrow 必須鎖 21.x:24.0.0 在 Windows + transformers 內某 import chain 觸發
#   access violation(Windows fatal exception、process silent crash)
# 21.0.0 是 datasets 5.0.0 最低要求 + 不會 access violation 的最後版本
# 詳細 root cause 見 docs/TTS_F5_INTEGRATION.md 「Phase 1.5.1c pyarrow silent crash」
pyarrow==21.0.0
pypinyin>=0.51.0

